const GL = WebGLRenderingContext;
export enum PixelType {
    u8 = GL.UNSIGNED_BYTE,
    u16 = GL.UNSIGNED_SHORT,
    u32 = GL.UNSIGNED_INT,
    s8 = GL.BYTE,
    s16 = GL.SHORT,
    s32 = GL.INT,
    float = GL.FLOAT,
}